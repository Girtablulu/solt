name       : solt
version    : 0.1.1
release    : 2
source     :
    - git|https://github.com/Girtablulu/solt.git : v0.1.1
license    : MIT
component  : programming.tools
summary    : simple helper bash script for packager
description: |
    simple helper bash script for packager
rundeps    :
    - git
    - powerline-fonts
    - silver-searcher
setup      : |
    %meson_configure
build      : |
    %ninja_build
install    : |
    %ninja_install
